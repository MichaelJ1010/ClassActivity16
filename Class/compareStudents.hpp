#ifndef COMPARESTUDENTS_H
#define COMPARESTUDENTS_H
#include <string>
using namespace std;

namespace compareStudents{
    string compareStudents(string student1, string student2, string students[],int grades[], int numberOfStudents);
}
#endif